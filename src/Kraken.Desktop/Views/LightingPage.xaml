<Page
    x:Class="Kraken.Desktop.Views.LightingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Kraken.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="LightingPage_OnLoaded"
    mc:Ignorable="d">
    <StackPanel x:Name="LedStackPanel" Spacing="2">
        <Border
            Padding="25,10"
            Background="{StaticResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{StaticResource CardStrokeColorDefaultBrush}"
            BorderThickness="1,1,1,1"
            CornerRadius="6 6 0 0">

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Bind KrakenDeviceName, Mode=OneWay}" />
        </Border>
    </StackPanel>
</Page>
