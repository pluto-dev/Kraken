<Page
    Background="#2b2b2b"
    Loaded="CoolingPage_OnLoaded"
    Title="Cooling"
    d:DataContext="{d:DesignInstance local:CoolingPage}"
    mc:Ignorable="d"
    x:Class="Kraken.Desktop.Views.CoolingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Kraken.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            FontSize="14"
            Grid.Row="0"
            HorizontalAlignment="Center"
            Text="{Binding Rpm}" />
        <sp:WpfPlot Grid.Row="1" x:Name="CoolingPlot" />
    </Grid>
</Page>